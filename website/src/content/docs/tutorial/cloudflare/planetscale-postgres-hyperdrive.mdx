---
title: Connect PlanetScale Postgres with Hyperdrive
description: Provision a PlanetScale Postgres database and front it with Cloudflare Hyperdrive so your Worker reaches it with edge-pooled, low-latency connections.
sidebar:
  order: 8
---

import { Code, Tabs, TabItem } from "@astrojs/starlight/components";

export const pgDeps = `@effect/sql-pg`;

This tutorial provisions a **PlanetScale Postgres** cluster —
managed Postgres with branch-per-PR workflows — and fronts it with
**Cloudflare Hyperdrive**, then binds the connection into your
Worker.

Hyperdrive is a Cloudflare-managed pooler that sits between
Workers and your origin database. The Worker sees a familiar
Postgres connection string, but the connection itself is already
pooled at the edge — no per-request TCP handshakes, no cold-start
connection storms.

> Prefer a different engine? See
> [Connect Neon with Hyperdrive](/tutorial/cloudflare/neon-hyperdrive)
> or
> [Connect PlanetScale MySQL with Hyperdrive](/tutorial/cloudflare/planetscale-mysql-hyperdrive).

## Add the PlanetScale provider

`Planetscale.providers()` is its own layer. Register it alongside
`Cloudflare.providers()` in your stack:

```diff lang="typescript"
// alchemy.run.ts
import * as Alchemy from "alchemy";
import * as Cloudflare from "alchemy/Cloudflare";
+import * as Planetscale from "alchemy/Planetscale";
+import * as Layer from "effect/Layer";

export default Alchemy.Stack(
  "MyStack",
  {
-    providers: Cloudflare.providers(),
+    providers: Layer.mergeAll(Cloudflare.providers(), Planetscale.providers()),
    state: Alchemy.localState(),
  },
  // ...
);
```

PlanetScale's auth uses an API token id + secret. `bun alchemy login`
adds a `Planetscale` step that either reads `PLANETSCALE_API_TOKEN_ID`,
`PLANETSCALE_API_TOKEN`, and `PLANETSCALE_ORGANIZATION` from the
environment (good for CI) or stores the credentials under
`~/.alchemy/credentials/<profile>/planetscale-stored.json`.

## Create the cluster

Create `src/Db.ts` with a `Planetscale.PostgresDatabase` — the
long-lived cluster:

```typescript
// src/Db.ts
import * as Planetscale from "alchemy/Planetscale";
import * as Effect from "effect/Effect";

export const Db = Effect.gen(function* () {
  const database = yield* Planetscale.PostgresDatabase("app-db", {
    region: { slug: "us-east" },
    clusterSize: "PS_10",
  });

  return { database };
});
```

`clusterSize` picks the underlying hardware tier; `region.slug`
pins where the data sits.

## Fork a branch

```diff lang="typescript"
  export const Db = Effect.gen(function* () {
    const database = yield* Planetscale.PostgresDatabase("app-db", {
      region: { slug: "us-east" },
      clusterSize: "PS_10",
    });

+   const branch = yield* Planetscale.PostgresBranch("app-branch", {
+     database,
+     isProduction: false,
+   });

-   return { database };
+   return { database, branch };
  });
```

`PostgresBranch` forks the cluster — cheap to spin up per preview
environment. `isProduction: false` flags this one as
non-production so it skips production safeguards (single-writer
locks, schema-change gates).

## Materialize a role for Hyperdrive

```diff lang="typescript"
    const branch = yield* Planetscale.PostgresBranch("app-branch", {
      database,
      isProduction: false,
    });

+   const role = yield* Planetscale.PostgresRole("app-role", {
+     database,
+     branch,
+     inheritedRoles: ["postgres"],
+   });

-   return { database, branch };
+   return { database, branch, role };
  });
```

`PostgresRole` materializes a user + password we can feed to
Hyperdrive. The role's `origin` (host / port / database / user /
password) is materialized lazily so the actual password never
lands in plan output.

## Put Hyperdrive in front

Hyperdrive needs an `origin` describing where your database lives.
`role.origin` is a pre-parsed `{ scheme, host, port, database,
user, password }` output ready to feed straight in:

```diff lang="typescript"
// src/Db.ts
+import * as Cloudflare from "alchemy/Cloudflare";
import * as Planetscale from "alchemy/Planetscale";
import * as Effect from "effect/Effect";

export const Db = Effect.gen(function* () {
  // ...
  return { database, branch, role };
});

+export const Hyperdrive = Effect.gen(function* () {
+  const { role } = yield* Db;
+  return yield* Cloudflare.Hyperdrive("app-hyperdrive", {
+    origin: role.origin,
+  });
+});
```

The `origin` is an `Output`, so alchemy orders `PostgresDatabase` →
`PostgresBranch` → `PostgresRole` → `Hyperdrive` on the deploy
graph.

## Install the client library

The Worker uses [`@effect/sql-pg`](https://effect.website/docs/sql/introduction)
— Effect's Postgres client (bundles [`pg`](https://node-postgres.com)
under the hood):

<Tabs syncKey="pkgManager">
  <TabItem label="bun" icon="bun">
    <Code code={`bun add ${pgDeps}`} lang="sh" />
  </TabItem>
  <TabItem label="npm" icon="npm">
    <Code code={`npm install ${pgDeps}`} lang="sh" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code={`pnpm add ${pgDeps}`} lang="sh" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code={`yarn add ${pgDeps}`} lang="sh" />
  </TabItem>
</Tabs>

## Bind Hyperdrive at init

`Cloudflare.Hyperdrive.bind(...)` resolves the runtime accessor for
the binding — `connectionString`, `host`, `port`, `user`,
`password`, `database`, plus a `raw` escape hatch. Yield it once at
init:

```diff lang="typescript"
// src/Api.ts
import * as Cloudflare from "alchemy/Cloudflare";
import * as Effect from "effect/Effect";
import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
+import { Hyperdrive } from "./Db.ts";

export default class Api extends Cloudflare.Worker<Api>()(
  "Api",
  { main: import.meta.path },
  Effect.gen(function* () {
+    const hd = yield* Cloudflare.Hyperdrive.bind(Hyperdrive);

    return {
      fetch: Effect.gen(function* () {
        return HttpServerResponse.text("ok");
      }),
    };
  }).pipe(Effect.provide(Cloudflare.HyperdriveBindingLive)),
) {}
```

Each accessor field (e.g. `hd.connectionString`) is itself an
`Effect` that requires `RuntimeContext` — it only resolves when a
real request is in flight, never at init.

## Derive a SQL client layer from the binding

Wrap `PgClient.layer({ url })` in `Layer.unwrapEffect` so layer
construction is deferred until the Effect it's provided to actually
runs. That's what reads `hd.connectionString` lazily — under the
request's runtime context, not at init.

```diff lang="typescript"
+import * as PgClient from "@effect/sql-pg/PgClient";
import * as Cloudflare from "alchemy/Cloudflare";
import * as Effect from "effect/Effect";
+import * as Layer from "effect/Layer";

  Effect.gen(function* () {
    const hd = yield* Cloudflare.Hyperdrive.bind(Hyperdrive);
+   const SqlLive = Layer.unwrapEffect(
+     hd.connectionString.pipe(
+       Effect.map((url) => PgClient.layer({ url })),
+     ),
+   );
```

`PgClient.layer` provides both the engine client (`PgClient`) and
the generic `SqlClient` tag, so downstream code can stay
engine-agnostic.

## Query inside `fetch` and provide the layer

Yield `SqlClient.SqlClient` inside `fetch` and run queries as
tagged templates. Provide `SqlLive` at the `fetch` boundary — that
placement is what makes the construction lazy, since the layer is
built when the request runs (with `RuntimeContext` in scope):

```diff lang="typescript"
+import * as SqlClient from "effect/unstable/sql/SqlClient";

    return {
      fetch: Effect.gen(function* () {
-       return HttpServerResponse.text("ok");
+       const sql = yield* SqlClient.SqlClient;
+       const rows = yield* sql`SELECT now() as now`;
+       return yield* HttpServerResponse.json({ ok: true, rows });
-     }),
+     }).pipe(Effect.provide(SqlLive)),
    };
```

Queries are themselves `Effect`s — you `yield*` them directly, no
`Effect.promise(async () => ...)` wrapper, and errors land in the
typed channel as `SqlError`.

## Wire the resources into the stack

Update `alchemy.run.ts` to yield `Db` and `Hyperdrive` so they
become part of the deploy graph:

```diff lang="typescript"
// alchemy.run.ts
import Api from "./src/Api.ts";
+import { Db, Hyperdrive } from "./src/Db.ts";

export default Alchemy.Stack(
  "MyStack",
  { /* ... */ },
  Effect.gen(function* () {
+    yield* Db;
+    yield* Hyperdrive;
    const api = yield* Api;
    return { url: api.url.as<string>() };
  }),
);
```

## Deploy

```sh
bun alchemy deploy
```

Watch the deploy plan: alchemy creates the PostgresDatabase, then
the branch, then the role, then the Hyperdrive that points at the
role's origin, then the Worker with the Hyperdrive binding
attached. After it completes, hit your Worker URL and you should
see something like:

```json
{ "ok": true, "rows": [{ "now": "2026-05-04T07:53:48.123Z" }] }
```

You're now talking to PlanetScale Postgres from the edge through
Hyperdrive. The next tutorial swaps the raw driver client for
**Drizzle ORM** and lets alchemy own migration generation as part
of every deploy.
